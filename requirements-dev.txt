# Development and system tools
# These should be installed separately, not via pip

# System tools (install via system package manager)
# - docker
# - kubernetes (kubectl)
# - helm
# - git

# Python development tools (can be installed via pip or pipx)
black>=23.10.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.0
pre-commit>=3.5.0
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Jupyter for development
jupyter>=1.0.0
jupyterlab>=4.0.0
ipywidgets>=8.1.0
nbconvert>=7.10.0

# Performance monitoring
memory-profiler>=0.61.0
py-spy>=0.3.0
psutil>=5.9.0
GPUtil>=1.4.0
